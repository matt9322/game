using UnityEngine;
using System.Collections;

public class RailingSystem : MonoBehaviour {
    public GameObject PlayerThrust;
    //private B1 GetThrust;
	// Use this for initialization
	void Start () {
        //GetThrust = PlayerThrust.GetComponent("Player1") as GetThrust;
    }
	
	// Update is called once per frame
	void Update () {
	
	}/*
    void OnCollisionEnter (Collision other)
    {
        if (other.gameObject.CompareTag("Player"))
        {

        }
    }*/
}
